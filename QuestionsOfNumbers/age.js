let age=parseInt(prompt("enter the age:"));
if(isFinite(age))
{
    if(age>=0 && age<=120)
    {
        let a=true;
        console.log(a);
    }
    else
    {
        let a=false;
        console.log(a);
    }
}
